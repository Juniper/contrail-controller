[DEFAULT]
test_path=${TEST_PATH:-./vnc_cfg_api_server/tests}
top_dir=./
# NOTE(ethuleau): The following insures that will group python tests cases
# together by class as each vnc_api_cfg_server test classes correspond to one
# API server instance. See the following for more details.
# https://stestr.readthedocs.io/en/latest/MANUAL.html#grouping-tests
parallel_class=True
