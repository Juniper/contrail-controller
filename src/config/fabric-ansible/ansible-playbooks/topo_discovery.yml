---
- name: Get the Topology Information from this device

  hosts: localhost
  gather_facts: no
  connection: local

  roles:
    - Juniper.junos
    - topology_discovery_role

  tasks:

    - set_fact:
        output: "{{output }}"
