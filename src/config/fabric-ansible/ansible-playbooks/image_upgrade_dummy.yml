- name: Image Upgrade
  hosts: localhost
  gather_facts: no
  connection: local

  tasks:
    - set_fact:
        output: {
              "status": "Success",
              "message": "Image upgrade for device sucessful"

          }

    - name: Print the output.
      debug:
        var: output
        verbosity: 1

