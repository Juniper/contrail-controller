---
# tasks file for juniper_junos_command

- name: EXECUTE COMMAND(S) ON A JUNIPER DEVICE
  juniper_junos_command:
    commands: "{{commands}}"
    display: "{{display}}"
    provider: "{{provider}}"
  register: device_data
