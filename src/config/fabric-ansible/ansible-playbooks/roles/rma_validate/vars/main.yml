---
prouter_uuid: "{{playbook_input.device_id}}"
prouter_name: "{{playbook_input.device_fqname[-1]}}"
prouter_vendor: "{{playbook_input.vendor | lower}}"
prouter_family: "{{playbook_input.device_family | lower}}"
prouter_product: "{{playbook_input.product_name}}"
prouter_os_version: "{{playbook_input.device_os_version}}"
dynamic_mgmt_ip_tbl: "{{playbook_input.input.get('dynamic_mgmt_ip_tbl')}}"
prouter_mgmt_ip: "{{playbook_input.device_management_ip}}"
device_mgmt_ip: "{{prouter_mgmt_ip}}"
device_name: "{{prouter_name}}"
device_vendor: "{{prouter_vendor}}"
username: "{{playbook_input.device_username}}"
password: "{{playbook_input.device_password}}"
timeout: "{{DEVICE_COMMAND_TIMEOUT | int}}"
device_username: "{{username}}"
device_password: "{{password}}"
