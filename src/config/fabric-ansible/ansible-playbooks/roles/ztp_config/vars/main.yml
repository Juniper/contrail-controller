---
# vars file for ztp_config role

job_ctx: {"auth_token": ""}
fabric_uuid: "{{ playbook_input.input.fabric_uuid }}"
device_count: "{{ playbook_input.input.device_count }}"
tftp_server: "{{ hostvars['localhost']['groups']['tftp'][0] }}"

