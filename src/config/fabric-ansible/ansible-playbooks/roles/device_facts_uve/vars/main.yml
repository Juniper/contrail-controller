---
# vars file for device-facts-uve

junos_username: "{{playbook_input.device_username}}"
junos_host: "{{playbook_input.device_management_ip}}"
prouter_name: "{{playbook_input.device_fqname[-1]}}"
junos_password: "{{playbook_input.device_password}}"
auth_token: "{{playbook_input.auth_token}}"

# vars for prouter

prouter_fqname: "{{playbook_input.device_fqname}}"
job_template_fqname: "{{playbook_input.job_template_fqname}}"
job_execution_id: "{{playbook_input.job_execution_id}}"
config_args: "{{playbook_input.args}}"
job_input: "{{playbook_input.input}}"
